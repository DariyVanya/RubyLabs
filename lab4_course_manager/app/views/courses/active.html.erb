<h1 class="mb-3">Active courses</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Budget</th>
      <th>Start date</th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= link_to course.title, course %></td>
        <td><%= number_to_currency(course.budget) %></td>
        <td><%= course.start_date %></td>
      </tr>
    <% end %>
  </tbody>
</table>
